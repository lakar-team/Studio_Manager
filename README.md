# Studio Manager - Local Git Studio

This is a local version control and AI context management studio.

## Features
- Commit history visualization
- AI context generation for large projects
- Integrated AI instruction storage

## Usage
Run `LocalGitRepo/launch.bat` to start the local studio at `http://localhost:8081/`.
